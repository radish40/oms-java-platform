# 实施记录

## 已执行

- 从已接入 Trellis 的仓库复制基础脚本和通用规范骨架。
- 将 Java `.gitignore` 从忽略整个 `.trellis/` 改为只忽略 `.trellis/.runtime/` 和 `.trellis/.developer`。
- 重写 Java 专属 `workflow.md`、后端规范索引和思考指南索引。
- 新增本任务目录，作为后续会话的入口。

## 待验证

- 运行 Trellis 任务列表命令。
- 扫描 `.trellis` 是否仍有模板乱码。
- 提交并推送远程。

